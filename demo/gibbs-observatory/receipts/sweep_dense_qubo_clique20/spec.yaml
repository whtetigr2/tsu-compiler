name: dense_qubo_clique20
description: >
  Dense all-pairs correlation QUBO / fully-connected portfolio factor
  (finance / stress class). Real problem: 20 binary assets with a coupling
  between every pair — the dense limit of Markowitz-style pairwise models
  and of all-to-all multiuser detection. Logically fine (IDEAL should pass)
  but degree 19 exceeds Z1's 16-port budget, so a Z1 owner MUST see a clean
  HARDWARE / degree_exceeded refusal. Uses tsu clique generator; not meant
  to be "fixed" by thinning without changing the problem statement.
generate:
  kind: clique
  n: 20
  weight: 1.0
contract:
  validate: []
